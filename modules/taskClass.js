// Create and export a tasks class which is an object
export default class tasksList {
    constructor(descrition, completed, index){
        this.description = '',
        this.completed = false,
        this.index = 0
    }
}